function getComponents () {}

// z = document.getElementsByTagName("component")
// elmnt.getAttribute("name")
// elmnt.getAttribute("src")
// elmnt.getAttribute("loaded")


// while els = document.getElementsByTagName("component")
//   for (el in els)
// try
// const attrs = [ 'name', 'src', 'loaded' ]
// el = attrs.reduce((o, k) => o[k] = el.getAttribute(k), {})
// ajax.get(el.src).then(r => el.innerHTML = r).catch(e => el.innerHTML = e)
